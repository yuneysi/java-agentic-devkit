# GitHub Copilot Instructions

Read `AGENTS.md` first and follow it strictly. `AGENTS.md` is the authoritative instruction file for this Java 8 project.

Use `AGENTS.md` as the Java 8 project reference.

Keep Copilot suggestions small, behavior-preserving, Java 8 compatible, and aligned with the repository's existing patterns.
