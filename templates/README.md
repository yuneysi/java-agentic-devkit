# Templates

This directory contains the template files that `java-agentic-devkit` applies to target Java projects.

See the root `README.md` for the template inventory, generated target-project structure, and recommended first commits.
